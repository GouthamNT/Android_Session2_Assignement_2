<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextAttributes">
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/no_height</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="TextViewStyle">
        <item name="android:layout_width">@dimen/no_width</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:paddingRight">@dimen/right_padding</item>
    </style>
    <style name="EditText">
        <item name="android:layout_width">@dimen/no_width</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">2</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColorHint">@color/hint</item>
    </style>

    <style name="ResultBox" parent="@style/EditText">
        <item name="android:background">@drawable/result_box</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>

    </style>

    <style name="ButtonAttributes" parent="@style/TextAttributes">
        <item name="android:gravity">center</item>
    </style>
    <style name="ButtonView">
        <item name="android:layout_width">@dimen/button_width</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
